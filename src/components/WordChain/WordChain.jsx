import styles from "./WordChain.module.css";

const WordChain = () => {
  return (
    <section className={styles.chain}>
      <h2>Cadena de palabras</h2>

      <p>Todavia no hay palabras</p>
    </section>
  );
};

export default WordChain;